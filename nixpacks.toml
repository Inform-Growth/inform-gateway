[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = [
  "pip install -e .",
  "npm install -g attio-mcp @modelcontextprotocol/server-github"
]

[start]
cmd = "PATH=/root/.nix-profile/bin:/nix/var/nix/profiles/default/bin:$PATH MCP_TRANSPORT=sse python remote-gateway/core/mcp_server.py"
