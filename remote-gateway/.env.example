# Remote Gateway Environment Variables
# Copy this file to .env and fill in your values.

# MCP Server
MCP_SERVER_NAME=[[ project_slug ]]
MCP_SERVER_HOST=0.0.0.0
MCP_SERVER_PORT=8000

# Exa (proxied via gateway — web search MCP)
# Get from: exa.ai → Dashboard → API Keys
# EXA_API_KEY=your_api_key_here

# Attio CRM (proxied via gateway — operators need no local credentials)
# Get from: Attio workspace settings → API Keys → Create API key (read+write scopes)
# ATTIO_API_KEY=your_api_key_here

# Apollo.io (direct REST API — no OAuth required)
# Get from: app.apollo.io → Settings → Integrations → API Keys
# APOLLO_API_KEY=your_api_key_here

# Google Workspace (Gmail + Calendar + Drive/Docs, proxied via gateway)
# OAuth client created in Google Cloud Console → APIs & Services → Credentials.
# First-time OAuth flow is browser-based (run locally; see README).
# WORKSPACE_MCP_CREDENTIALS_DIR points at a persistent location so cached
# tokens survive container restarts (Railway: mount the volume at /data and
# set this to /data/google-workspace-creds).
# GOOGLE_OAUTH_CLIENT_ID=your-client-id.apps.googleusercontent.com
# GOOGLE_OAUTH_CLIENT_SECRET=GOCSPX-your-secret
# USER_GOOGLE_EMAIL=you@example.com
# WORKSPACE_MCP_CREDENTIALS_DIR=/data/google-workspace-creds
# Optional: base64 of the cached <USER_GOOGLE_EMAIL>.json. Used by the gateway
# to seed WORKSPACE_MCP_CREDENTIALS_DIR on first deploy when the volume is
# empty; existing files are never overwritten. Generate with:
#   base64 -i ~/.google_workspace_mcp/credentials/<email>.json | tr -d '\n'
# GOOGLE_WORKSPACE_CREDS_B64=

# Database — PostgreSQL connection string. Tool-call history and task metadata
# are stored here. Railway injects this automatically when a Postgres plugin is
# added to the service. For local dev, use a local PostgreSQL instance.
# DATABASE_URL=postgresql://user:password@localhost:5432/gateway

# Data Source API Keys
# STRIPE_API_KEY=sk_live_...
# SNOWFLAKE_ACCOUNT=...
# SNOWFLAKE_USER=...
# SNOWFLAKE_PASSWORD=...
# CRM_API_KEY=...
# CRM_BASE_URL=https://api.example.com

# GitHub (required for notes tools)
# GITHUB_TOKEN=github_pat_...
# GITHUB_REPO=owner/repo
# GITHUB_BRANCH=main
# NOTES_PATH=notes

# GitHub — deployment repo (required for report_issue / list_my_issues)
# Separate from GITHUB_REPO (the notes repo). This is the repo that receives
# real GitHub Issues filed by agents during task execution.
# INFORM_GATEWAY_DEPLOYMENT_REPO=Inform-Growth/your-client-gateway
# INFORM_GATEWAY_GITHUB_TOKEN=github_pat_...  (needs issues:write on deployment repo)
# INFORM_GATEWAY_REPORT_ISSUE_DISABLED=false  # set to "true" to disable issue filing

# OpenAI (used by CI/CD QA agent)
# OPENAI_API_KEY=sk-...
